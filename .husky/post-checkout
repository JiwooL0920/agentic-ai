#!/bin/sh
# LNAI: Regenerate AI configs after checkout/branch switch

if [ -d ".ai" ] && command -v lnai >/dev/null 2>&1; then
    echo "🔄 Syncing AI configs..."
    lnai sync --quiet 2>/dev/null || lnai sync
fi
